# R3 门控与热退化联合臂设计提案

状态：阶段 0，待审批；继承R1和R2全部待决项。预期主轴：热退化鲁棒。

## 接入与结构

R1训练增强 + **完全同一份**R2第一层门控。其他任何参数不增不改；不能为R3单独优化门宽度或退化概率。

```mermaid
flowchart LR
 X["原训练增强 / RGB不变"] --> A["R1热退化，同seed配对随机流"]
 A --> R["R2第一层RGB/T精确切片 + 热空间门"]
 R --> B["原共享BN / 第二层stem / PIDNet / 原损失"]
```

接入位置 `G:/py2/src/flame3_dataset.py:255` 与 `G:/py2/third_party/RoboFireFuseNet/models/pidnet.py:26`，详见R1/R2独立页。评估时增强关闭，沿统一val47扰动函数，不叠加两遍热退化。

## 预算

与R2一致：7,624,164部署参数，增量225，GMAC代理7.489264；相对基线+0.018350080 G。训练增强代价不计入推理预算；训练/推理尚未执行。

## 历史与2x2归因

不等于历史MRFF+NTS，R2位置、算子、初始化目的不同，R1为明确热退化而非默认继承旧NTS。历史限制与证据见R2；不能将二者组合当作独立原创。

固定因子表：baseline=(门0,增强0)，R1=(0,1)，R2=(1,0)，R3=(1,1)。对每seed同一评估条件下的原始Smoke IoU `Y`，记录增强效应`Y_R1-Y_B0`，门效应`Y_R2-Y_B0`，交互`Y_R3-Y_R2-Y_R1+Y_B0`。抗退化指标先定义`D=IoU_clean-IoU_noise`，改善为`D_B0-D_arm`，不能把正负方向混用。交互只解释归因，**不新增通过阈值或替代3/3规则**。

## 待审批与工程门

R1细节或R2门未定时，本臂也不能启动。所有未运行结果为空，不填0、不用其他seed均值补齐。阶段1的初始等价检查关闭训练退化并用相同合成输入，另测增强的RGB不变及配对性；不能用不同输入比较所谓零初始化等价。
