# E1 窄分割头工程对照设计提案

状态：阶段 0，待审批；明确不计创新。

## 接入与结构

只把 `final_layer` 中间通道128改64；输入128、输出三类保持。不能改全局`head_planes`连带缩小`seghead_p`。位置 `G:/py2/third_party/RoboFireFuseNet/models/pidnet.py:90,94`；`pidnet_utils.py:89`。

```mermaid
flowchart LR
 X["原DFM:128x64x80"] --> A["BN128 / ReLU"]
 A --> B["3x3:128->64, bias=False"]
 B --> C["BN64 / ReLU"]
 C --> D["1x1:64->3, bias=True"]
 D --> Y["原输出与上采样协议"]
```

仅一个窄头版本；不组合剪枝、量化、TensorRT或另行蒸馏。保持辅助head与所有损失。新shape权重按同一初始化协议构建，不截取训练权重制造非对称起跑。

## 预算

原final head148,355参数，新74,307，净减74,048；整网7,549,891。原头Conv MAC0.756940800 G，新0.378470400 G，净减0.378470400；整网代理7.092444 GMACs，-5.066%。只在真实计数达到5%且精度护栏通过时满足工程R3，当前不能判为通过。

## 历史与定位

这是宽度对照，不等于LSCM/DWR/BGAF等机制创新。与GCBlock不同：E1牺牲部署头容量，S2训练多路部署折叠。保留E1是为了避免把所有容量减少带来的现象都解释成新机制。

## 待审批与工程门

“只跑一次参照”与阶段2“9臂三seed”冲突。总表暂预留200/201/202，**不是授权三次**；建议明确为seed200工程参照，或书面确认三seed。若单seed，只能描述工程观测与单seed护栏，不能伪造3/3、SD=0或跨seed稳定性；若需要与三seed效率通过规则对应，应明确此规则是否适用于单seedE1。
