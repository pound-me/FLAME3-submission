# 阶段 2 授权与口径补充

日期：2026-09-18。补充原 v2，不改写原文件或既有失败记录。

用户授权：S3、S4、S6、R1、B1、E1 即可开始三种子 200/201/202、各 30 轮；R2/R3 在按新容差通过 CPU+CUDA 第 5 门并完成两轮工程训练后加入；S1 仅诊断、不训练；正式延迟空闲补测，不阻塞训练。

## 本次冻结

- 初始队列只有 S3/S4/S6/R1/B1/E1，共 18 次，从各 seed 的初始模型开始，不从工程 checkpoint 热启动。
- 每臂按 200、201、202 顺序运行，臂间顺序 S3、S4、S6、R1、B1、E1；GPU 仅一个队列，不并发训练。
- 训练内核、数据划分、增强、batch8、AMP、clip5、SGD、poly100、预训练且随机四通道 stem 保持 v1.1。
- 以 epoch26–30 的逐指标均值为主口径。保留这五个完整 checkpoint，last 也保留；不以两轮工程 loss 选择方案。
- Smoke 精度轴：干净 Smoke IoU 提升 >=0.020，3/3 种子。
- 鲁棒轴：thermal_noise sigma=.05 的 Smoke IoU 衰减比配对基线少 >=0.030，3/3 种子。其余 .02/.10、thermal_zero、rgb_zero 仍记录。
- 原 No-Fire 联合 FP 护栏（绝对 <=.005、配对增加 <=.001）和干净 Smoke IoU >=配对基线-.010 不变。
- No-Fire 护栏沿原冻结验证器的 val134 内 35 张 No-Fire；不把 val47 误作 No-Fire 全样本池。
- Fire、三类 mIoU、Background、S 记录；S 的提升分支不再参与通过判定，原 S 跨种子偏离停机规则也不使用。best_S 仅记录 epoch/数值，不选择训练权重或训练时长。
- 没有新增 Smoke 异常停止阈值；非有限 loss/梯度/参数或异常退出立即停止该臂后续种子，其他获批臂可继续。不根据验证趋势提前截断正常的 30 轮。
- E1 依本次明确授权同样三种子；仅工程对照，不计结构创新。效率轴仍按原 E1 规则，正式效率结论等待空闲补测。
- 阶段 2 运行干净验证，鲁棒比较和最终判定在阶段 3，不在队列中自动启动 test 或重新训练基线。
- 不读取 test107 图像、掩膜、预测、统计量；只读 train493、val134 和 A001–A047 人工目标。原冻结源码/配置/标签/checkpoint 不写入。
- 所有新输出放独立 stage2 目录；不打开桌面应用，不结束他人进程，不修改服务、代理、防火墙或调度任务。

## 明确待补

R2/R3 的“新容差”具体数值及 CPU/CUDA FP32/AMP 口径未在现存工单或本条消息给出，不能猜测。当前六臂队列没有 R2/R3；等待用户补充后单独执行工程门并做追加清单。

S1 允许对同一模型和输入做数值诊断，包括记录默认精度与关闭 TF32 的差异，以及局部双精度代数检查。诊断不能反向覆盖原失败或产生训练资格。
