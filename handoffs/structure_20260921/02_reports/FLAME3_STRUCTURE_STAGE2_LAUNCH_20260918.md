# FLAME3 阶段 2 启动记录

日期：2026-09-18。性质：已经启动的训练队列记录，不是训练完成或方法有效性报告。

## 已启动

用户于本次会话授权 S3、S4、S6、R1、B1、E1 现在启动三种子 200/201/202、每种子 30 轮，正式延迟不阻塞。独立队列于 2026-09-18 17:16:39 左右启动。

- 队列顺序：S3 -> S4 -> S6 -> R1 -> B1 -> E1，每臂依次 seed200/201/202，共 18 次，GPU 串行。
- 快照时间：2026-09-18T17:19:35.316903（4090 本地时间）；S3 seed200 已完成 6 轮，正在第 7 轮。
- 已确认完整训练/验证 epoch 和正式 last.pth 写入，队列未报告失败。不是只有进程启动提示。
- 队列 PID 57532，首个训练 PID 44556。PID 仅作该次启动证据，后续查状态文件，不据 PID 盲目终止进程。
- 用 SSH 直接启动无窗口后台进程；60 秒心跳测试在 SSH 会话退出后继续并正常完成。没有打开桌面应用，没有关闭他人程序。

## 冻结训练协议

- train493、val134、人工 A001–A047；原 No-Fire 护栏仍沿冻结验证器使用 val134 中 35 张 No-Fire。
- 物理 batch8、4 workers、512x640、AMP、SGD、clip L2=5、poly 总周期100、原几何增强。
- 每个正式 seed 重新创建其初始化模型：匹配 301 个 ImageNet 预训练张量，只跳过 conv1.0.weight 的随机四通道 stem。
- 不读取或加载两轮工程 checkpoint 作为正式训练起点；恢复入口拒绝工程 checkpoint。
- 每轮训练完整 61 batch/488 条（drop_last=True），验证完整 17 batch/134 张。
- 新模块初始化使用各自 seed，不推进 CPU/CUDA 公共 RNG；R1 仅叠加已批准的热退化，RGB 不变。
- 每轮保存原子更新的 last.pth，含 optimizer/scaler/RNG；另保留 epoch26–30 五个 checkpoint 和对应日志。没有按早期验证趋势改变 30 轮时长。
- 原训练内核和验证器复用源码快照；冻结基线源码、原配置、标签和 checkpoint 不修改。

## 修订判定

- 主比较为 epoch26–30 均值。Smoke 精度轴为干净 Smoke IoU 提升 >=0.020，3/3 种子；鲁棒轴为 sigma=.05 热噪声下的 Smoke 衰减比配对基线少 >=0.030，3/3 种子。
- 原干净 Smoke 与 No-Fire 联合 FP 护栏不变；.02/.05/.10、thermal_zero、rgb_zero 扰动条件不变。
- S 只记录，不以 S 提升通过，也不以原跨种子 S 偏离规则停机。best_S 只记录 epoch/值，不选训练权重或时长。
- Fire/Heat 侧不参与正向判定；No-Fire 联合误报护栏按原条款保留。Background、三类 mIoU 为记录项。
- 非有限 loss/梯度/参数、异常退出立即停止该臂后续种子，其他获批臂继续；未另设 Smoke 早停阈值。
- E1 本次明确为三种子，只算工程对照。正式延迟待空闲补测，不阻塞训练。
- 本队列只进行阶段 2 干净验证。阶段 3 鲁棒对照、配对判定尚未执行，不把早期指标当作方法结论。

## R2/R3 待补

用户允许按新容差重新执行第5门，但本条消息和现存 v2 文件没有给出数值，也未明确 CPU/CUDA FP32/AMP 是否同一容差。已在会话询问，未自行取值。

R2/R3 当前没有入队或重新训练；待新容差明确，再作同 CPU 检查加 CUDA，工程门通过后补两轮，再追加正式队列。原失败证据继续保留，不覆盖。

## S1 仅诊断

| 合成诊断 | 三个固定输入最大主输出差 |
|---|---:|
| 原 FP32，cuDNN TF32 开启 | 0.0064554214477539062 |
| 诊断 FP32，仅关闭 TF32 | 1.33514404296875e-05 |
| 原 AMP | 0.03125 |

局部 scale_process 双精度重新折叠最大差：8.3266726846886741e-16。关闭 TF32 的 FP32 结果明显更小、局部双精度误差很小，支持数值精度/累加影响的解释，但并不能据此宣称原 AMP 折叠门通过。原 1e-4 失败记录未变，S1 训练次数=0，eligible_for_training=false；诊断结束恢复原 TF32 设置。

## 核验与文件

- 8 项本地合同测试通过：配置差异、18次队列、数据访问边界、test/predict路径拒绝、固定窗口与S隔离、非有限数值拒绝、受限checkpoint恢复、三种子初始化/RNG隔离。
- 37 个部署源码/证据文件逐项 SHA256 核验。
- 预检记录 1932 项训练/验证输入 SHA256；其中 train493 输入与阶段1一致。
- 20 个启动快照文件本地逐项核验；40 个冻结输入 SHA256 再核验未变。
- 未读取 test107 图片、标签、预测或统计量；父进程及 DataLoader worker 限定文件允许列表，不调用test入口。这是代码/日志/输入清单证据，不是操作系统全盘监控。

源码包 SHA256：`01020F5B6A70A6DBC0992EC68AE517B31FACBF3580522B49565064B40D528DF0`。
源码 MANIFEST SHA256：`526B2A234D6565556C40FEF9F8B7BA53DE9DB3EFCFB1C827D5A3AC668056C522`。
本次授权记录 SHA256：`C0F851FC264EEE85EE672BE4F176A247E330A6BF7C05E2744407AF5998BAA09B`。
启动快照 ZIP SHA256：`A18652122C43899521E8C634056D31A953127FDEAD698DE4BC488435681439DA`。

远端实时状态：`D:/qianpengcheng/7.31/flame3_4090_bundle_v1_20260731/stage2_structure_20260918_v1/QUEUE_STATUS.json`。
单次运行：同目录 `runs/<arm>/seed<seed>/STATUS.json`、`metrics.jsonl`、`last.pth`。
日志：同目录 `logs/<arm>_seed<seed>.log`。
本地交付：`G:/py2/experiments/flame3_structure_screen_20260917/stage2/launch_20260918/`。

G 盘和本报告中的状态是时间戳快照，不是自动实时镜像。后台队列继续运行；下次查询应以远端 QUEUE_STATUS 和各 run 状态为准。本次未设置定时器、桌面任务或未经授权的通知服务。
